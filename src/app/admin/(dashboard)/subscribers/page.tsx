import{createClient}from"@/lib/supabase/server";import{SubscribersTable}from"@/components/admin/InboxTables";import type{SubscriberRow}from"@/types/database";
export default async function SubscribersPage(){const supabase=await createClient();const{data}=await supabase.from("subscribers").select("*").order("subscribed_at",{ascending:false});const rows=(data as SubscriberRow[]|null)??[];return <div><h1 style={title}>Subscribers</h1><p style={note}>{rows.filter(x=>x.status==="active").length} active subscribers · Export from Supabase when needed.</p><div style={{marginTop:"42px",overflowX:"auto"}}><SubscribersTable rows={rows}/></div></div>};const title:React.CSSProperties={margin:0,fontFamily:"var(--font-anton)",fontSize:"52px",fontWeight:400,textTransform:"uppercase"};const note:React.CSSProperties={fontFamily:"var(--font-geist-mono)",fontSize:"11px",color:"var(--fg-muted)"};
