import { createClient } from "@/lib/supabase/server";import TeamManager from "@/components/admin/TeamManager";import type{TeamMemberRow}from"@/types/database";
export default async function TeamAdminPage(){const supabase=await createClient();const{data}=await supabase.from("team_members").select("*").is("deleted_at",null).order("display_order");return <div><h1 style={title}>Team</h1><p style={note}>Manage portraits, roles, biographies, order, and visibility on the About page.</p><div style={{marginTop:"48px"}}><TeamManager members={(data as TeamMemberRow[]|null)??[]}/></div></div>}
const title:React.CSSProperties={margin:0,fontFamily:"var(--font-anton)",fontSize:"52px",fontWeight:400,textTransform:"uppercase"};const note:React.CSSProperties={margin:"8px 0 0",fontFamily:"var(--font-geist-mono)",fontSize:"11px",color:"var(--fg-muted)"};
