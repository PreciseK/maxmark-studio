import{createClient}from"@/lib/supabase/server";import{ContactsTable}from"@/components/admin/InboxTables";import type{ContactSubmissionRow}from"@/types/database";
export default async function ContactsAdminPage(){const supabase=await createClient();const{data}=await supabase.from("contact_submissions").select("*").order("created_at",{ascending:false});const rows=(data as ContactSubmissionRow[]|null)??[];return <div><h1 style={title}>Contacts</h1><p style={note}>{rows.length} submissions · Track every website inquiry.</p><div style={{marginTop:"42px",overflowX:"auto"}}><ContactsTable rows={rows}/></div></div>};const title:React.CSSProperties={margin:0,fontFamily:"var(--font-anton)",fontSize:"52px",fontWeight:400,textTransform:"uppercase"};const note:React.CSSProperties={fontFamily:"var(--font-geist-mono)",fontSize:"11px",color:"var(--fg-muted)"};
