import{createClient}from"@/lib/supabase/server";import{BookingsTable}from"@/components/admin/InboxTables";import type{BookingRow}from"@/types/database";
export default async function BookingsPage(){const supabase=await createClient();const{data}=await supabase.from("bookings").select("*").order("booking_date",{ascending:true}).order("start_time",{ascending:true});const rows=(data as BookingRow[]|null)??[];return <div><h1 style={title}>Bookings</h1><p style={note}>{rows.filter(x=>x.status==="pending").length} requests waiting · Confirm, complete, or cancel studio sessions.</p><div style={{marginTop:"42px",overflowX:"auto"}}><BookingsTable rows={rows}/></div></div>};const title:React.CSSProperties={margin:0,fontFamily:"var(--font-anton)",fontSize:"52px",fontWeight:400,textTransform:"uppercase"};const note:React.CSSProperties={fontFamily:"var(--font-geist-mono)",fontSize:"11px",color:"var(--fg-muted)"};
